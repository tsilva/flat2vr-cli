interface:
  display_name: "Build Release"
  short_description: "Build and publish validated flat2vr-cli distributions"
  default_prompt: "Use $build-release to build or publish a flat2vr-cli PyPI release and verify it."
