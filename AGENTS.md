# Flat2VR Repository Instructions

## Releases

Use `$build-release` from `.codex/skills/build-release/SKILL.md` for release
candidate builds, version preparation, PyPI publication, release monitoring,
and post-publication verification. Use only the checked-in trusted-publishing
workflow; never upload distributions manually or expose PyPI credentials.
